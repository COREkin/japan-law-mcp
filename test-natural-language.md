# japan-law-mcp 自然言語テスト

Kiro / Claude Desktop のチャットに1つずつ貼り付けて結果を確認する。
全て正常に返ればOK。"Failed to call MCP tool" が出たらバグ。

---

## 1. search_laws

```
民法という法令を検索して
```

→ 民法がヒットすること

```
存在しない法律ABCを検索して
```

→ 0件。エラーではないこと

---

## 2. search_by_keyword

```
損害賠償というキーワードで法令本文を検索して
```

→ 民法等がヒット

---

## 3. find_article

```
民法第709条の条文を教えて
```

→ 不法行為による損害賠償の条文テキスト

```
刑法第199条の条文を教えて
```

→ 殺人罪の条文

```
民法第9999条の条文を教えて
```

→ 存在しませんメッセージ。エラーではないこと

```
民法第1条の2を教えて
```

→ 枝番条文が正しく取得されること

---

## 4. get_law_content

```
少年法の全文を見せて
```

→ 全文テキスト。章・条の構造あり

---

## 5. get_law_revisions

```
民法の改正履歴を教えて
```

→ 複数の改正が時系列で返る

---

## 6. batch_find_articles

```
民法第709条と刑法第199条を一括で取得して
```

→ 2件とも条文が返る

```
民法第709条と刑法第9999条を一括で取得して
```

→ 709は成功、9999は存在しない。エラーではないこと

---

## 7. verify_citations ⚠️ 重点

```
この文章の法令引用が正しいか検証して: 民法第709条に基づき損害賠償を請求する
```

→ ✅ 実在。"Failed to call MCP tool" が出ないこと

```
この文章の法令引用が正しいか検証して: 刑法第9999条により処罰される
```

→ ❌ 不在だが、**正常応答**として返ること。"Failed" 表示なし

```
この文章の法令引用が正しいか検証して: 民法第709条に基づき損害賠償を請求し、刑法第9999条により加重処罰される
```

→ 1実在 + 1不在の混在結果。正常応答であること

```
この文章の法令引用が正しいか検証して: 今日は天気がいい
```

→ 法令引用が検出されませんでしたメッセージ

```
この文章の法令引用が正しいか検証して: 労働基準法第36条第1項に基づく協定
```

→ ✅ 実在（36協定）。項レベルまで検証

---

## 8. action_plan

```
残業代が払われない場合どうすればいい？
```

→ 5ステップガイド。労働基準法が関連法令に含まれる

```
敷金を返してもらえない
```

→ 借地借家法・民法が関連法令に含まれる

---

## 9. get_cache_stats

```
japan-law-mcpのキャッシュ統計を見せて
```

→ hit/miss/size の統計

---

## 10. clear_cache

```
japan-law-mcpのキャッシュをクリアして
```

→ クリア成功メッセージ

---

## エッジケース

```
この文章の法令引用が正しいか検証して: （ここに2000字以上の長文を貼る）
```

→ バリデーションエラー（上限超過）。クラッシュしないこと

```
民法の条文を教えて（条番号なし）
```

→ 条番号が必要というメッセージ or 全文案内。クラッシュしないこと
